package com.example.facturas.utils

import java.time.LocalDate

object Dates {
    val EPOCH_DATE: LocalDate = LocalDate.ofEpochDay(0)
    val NOW: LocalDate = LocalDate.now()
}

val WEB_PAGE_URL = "https://www.iberdrola.es"